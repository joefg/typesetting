% !TEX TS-program = pdflatex
%
% Adapted from https://github.com/mikedecr/latex-business-card,
% Uses less stuff.
%
% You could use xelatex for easier font customization,
%   but it requires more intervention to make FontAwesome work.
%   see (e.g.) https://tex.stackexchange.com/questions/132888/fontawesome-font-not-found: 
%

\documentclass[10pt, oneside, final]{article}

\usepackage{parskip}  % remove paragraph indents
\usepackage{graphicx} % include support for graphics
\usepackage{setspace}

% use sans-serif font by default
\renewcommand{\familydefault}{\sfdefault}

% set all margins to 0 and set business card size
\usepackage[
  paperwidth = 3.5in,
  paperheight = 2in,
  margin = 0.2in,
  noheadfoot
]{geometry}

% interline spacing
\setlength{\baselineskip}{0cm} % between baselines
\setlength{\topskip}{0pt}      % between header and text block

% wrapping text around figure
%\usepackage{wrapfig}

% Double space everything
\doublespacing

\begin{document}
	% Front
	\thispagestyle{empty}

	\centerline{\underline{\large\textbf{Your Name Here}}}
	\centerline{Your Strapline}

	\vfill
	\texttt{012345 67890}\hfill
	\texttt{example.com}\hfill
\end{document}
